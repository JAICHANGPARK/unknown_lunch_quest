class Bulletin{
  final String mainKey;
  final String title;
  final String content;
  final String datetime;
  final int commentCount;

  Bulletin({this.mainKey, this.title, this.content, this.datetime, this.commentCount});
}